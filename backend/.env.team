# Lost & Found Campus - Team Environment Template
# Copy this file to .env and add your team's shared credentials

# JWT Configuration
JWT_SECRET=team-jwt-secret-key-change-for-production

# Database Configuration - PostgreSQL (SHARED FOR ALL TEAM MEMBERS)
# All team members use the same database user and database
DB_HOST=lost-found-db-do-user-9161531-0.g.db.ondigitalocean.com
DB_NAME=lost_found_campus
DB_USER=doadmin
DB_PASSWORD=AVNS_Ejf793MIWzW8Bkelr7G
DB_PORT=25060

# Legacy JSON Database Configuration (for migration only)
DATABASE_FILE=lost_found_db.json
USER_DATABASE_FILE=user_db.json

# Server Configuration
PORT=8000
DEBUG=true

# AWS S3 Configuration (REQUIRED - Team shared credentials)
# All team members use the same AWS S3 bucket for images
# PRIORITY: S3 first, local storage as backup only
# REPLACE WITH YOUR TEAM'S REAL AWS CREDENTIALS:
AWS_ACCESS_KEY_ID=AKIATYHYWIUMBNR3TP7S
AWS_SECRET_ACCESS_KEY=LJvJuyXHDiNd8o5OVuqPGTLgiJizVgm8XvgVDdJr
AWS_S3_BUCKET_NAME=lost-found-campus-photos
AWS_REGION=ap-southeast-1

# Upload Configuration
MAX_FILE_SIZE=5242880  # 5MB in bytes
ALLOWED_EXTENSIONS=jpg,jpeg,png,gif,webp
UPLOAD_DIR=uploads

# Admin Configuration (same for all team members)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# CORS Configuration
FRONTEND_URL=http://localhost:3000

# Team Notes:
# - All 3 members should use IDENTICAL settings above
# - Run setup_team_database.py once per machine
# - Run seed_database.py to populate with sample data
# - Images will be stored in shared S3 bucket (priority)
# - Local storage is backup only when S3 fails
# - Database is shared among all team members
